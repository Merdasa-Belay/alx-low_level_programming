#include <stdio.h>
/**
 * main - rename the program
 * @argv: stores string
 * @argc: stores the number of command line
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
(void)argc;
return (0);
}
